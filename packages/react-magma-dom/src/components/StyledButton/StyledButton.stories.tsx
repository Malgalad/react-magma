import React from 'react';
import { StyledButton } from '.';

export default {
  component: StyledButton,
  title: 'StyledButton',
};

export const Default = () => {
  return < StyledButton isLoading isInverse> text </ StyledButton >;
};
